export type Project = {
  title: string;
  techStack: string[];
  description: string;
  github?: string;
  demo?: string;
};

export type Experience = {
  companyName: string;
  image: string;
  shortDescription: string;
  // date: string;
  // longDescription: string;
} 